# Засварыг ажиллуулах дараалал

## Нэгтгэхээс өмнө

- Өгөгдлийн сан болон оруулсан зургуудын нөөц хуулбар авна.
- Одоогийн schema нь **SQLite**. `DATABASE_URL` нь persistent volume дахь SQLite файл руу заасан байх ёстой. Шинэ хоосон сан руу санамсаргүй солихгүй.
- `JWT_SECRET` нь 32-оос доошгүй тэмдэгттэй, санамсаргүй нууц утга байна. Соливол өмнөх админ болон хэрэглэгчийн нэвтрэлт хүчингүй болж, дахин нэвтэрнэ.
- Анхны админ үүсгэхэд `ADMIN_USERNAME`, `ADMIN_PASSWORD` шаардлагатай. Өмнө үүссэн админы нууц үгийг код автоматаар солихгүй.

## Сагсны schema шинэчлэл

Customer хүснэгтэд `cartItems` (анхны утга `[]`), `cartVersion` (анхны утга `0`) нэмэгдсэн. Код deploy хийхээс өмнө нөөцөө шалгаад төслийн зөв санг заасан орчинд:

```sh
npx prisma db push
npx prisma generate
```

`--accept-data-loss`, `db reset`, seed ажиллуулах шаардлагагүй. Өгөгдөл устгах анхааруулга гарвал үргэлжлүүлэхгүй. Эдгээр команд нь hosting үйлчилгээний ердийн dependency орчинд ажиллана.

Нэвтрээгүй зочны сагс браузерт; нэвтэрсэн хэрэглэгчийн сагс серверт хадгалагдана. Нэвтрэхэд зочны сагс нэг удаа нийлнэ. Гарахад тухайн хэрэглэгчийн сагсыг дэлгэцээс цэвэрлэнэ. Өөр төхөөрөмжийн өөрчлөлт цонхонд буцаж ороход эсвэл 30 секунд тутам шинэчлэгдэнэ. Зэрэг зассан хувилбар зөрчилдвөл хуучнаар дарж бичихгүй, шинэчилж дахин үйлдэл хийхийг мэдэгдэнэ. Сүлжээ тасарсан үед серверт хадгалагдсан гэж үзэхгүй.

## Зураг хадгалалт

- `UPLOAD_DIR`-ийг **байнгын volume** дахь зургийн хавтас руу заана (жишээ `/data/uploads/products`). Зөвхөн variable үүсгэх нь volume үүсгэсэн гэсэн үг биш.
- Хуучин зургууд байвал өмнөх `public/uploads/products` хавтаснаас шинэ хавтас руу нөөцтэй нь шилжүүлнэ. URL нь `/uploads/products/<filename>` хэвээр.
- JPEG/PNG/WebP/GIF, 5MB хүртэл. Сервер WebP болгон хөрвүүлнэ; animated GIF-ийн эхний кадрыг хадгална.

## Төлбөр

- Backend Wire.mn хэвээр; Qpay нь хэрэглэгчид харагдах нэр.
- `WIRE_MN_API_KEY`, `WIRE_MN_ALLOWED_OPERATORS`, `WIRE_MN_WEBHOOK_SECRET`, `NEXT_PUBLIC_SITE_URL=https://socialtool.store` тохируулна.
- Live key ашиглахад sandbox операторыг хориглоно. Операторын ID-г Wire dashboard-аас авна; тааж оруулахгүй.
- Webhook: `https://socialtool.store/api/payment/wire/callback` — Wire dashboard дээр Verified / Enabled.
- Кодын болон mock шалгалт бодит гүйлгээний баталгаа биш. Төлбөрийн оператор, орлого авах данс, merchant эрхийг сервер/үйлчилгээний эзэмшигч шалгах шаардлагатай.

## Telegram

- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ADMIN_CHAT_ID`-ийг серверийн нууц Variables-д оруулна; GitHub, нийтэд харагдах settings, чатад токен оруулахгүй.
- Админ боттой яриа эхлүүлж `/start` илгээнэ. Групп бол ботыг нэмээд тохирох chat ID/эрхийг ашиглана.
- Админ → Тохиргоо → **Холболт шалгах**: getMe/getChat-аар бот, чат олдох эсэхийг шалгана. Мессеж илгээхгүй; төлбөр хийхгүй.
- Энэ нь админд захиалга/төлбөр/чат мэдэгдэл хүргэх холболт. Telegram дээрээс дэлгүүр удирдах хоёр талын бот шинээр хийгдээгүй.

## Шалгалт

- `npx tsc --noEmit`
- `npx next build` (TypeScript алдаа нуух тохиргоог унтраасан)
- Node 24: `node --test tests/media.test.mjs tests/wire-signature.test.mjs`
- Production API интеграцийн тест: эхлээд **тусгаарласан `/tmp/` SQLite сан** дээр Prisma schema үүсгэнэ. Дараа нь `TEST_DATABASE_URL=file:/tmp/<test-db>.db node --test tests/integration.test.mjs`. Бодит сан руу тест ажиллуулахгүй.
- Локал шалгалт: бүртгэл, давхардсан и-мэйл, нэвтрэлт, админ session, зураг upload/read, буруу зураг, хоёр нэвтрэлт дэх ижил сагс, өөр хэрэглэгчийн тусгаарлалтыг шалгасан.
- Production дээр deploy, бүх browser харагдац, YouTube-ийн тухайн видео embeddable эсэх, бодит төлбөр болон Telegram мэдэгдэл хүргэлтийг тусад нь шалгана.

Нийтийн сайтад хуучин Wire.mn текст харагдаж байсан тул GitHub дахь main commit болон ажиллаж буй deployment commit ижил эсэхийг заавал шалга.
